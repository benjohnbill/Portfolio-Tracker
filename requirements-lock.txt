# Seed lock file for cross-device bootstrap.
# Canonical backend deps live in backend/requirements.txt.
# After first successful local bootstrap, run:
#   .\tools\bootstrap_env.ps1 -WriteLock
# to freeze exact versions on this machine.

-r backend/requirements.txt

